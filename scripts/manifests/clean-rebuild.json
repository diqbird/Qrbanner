{
  "upload": [],
  "yarn_install": false,
  "prisma_generate": false,
  "prisma_migrate": false,
  "build": false,
  "restart": false,
  "extra_commands": [
    "cd /var/www/qrbanner && rm -rf .next && yarn build 2>&1 | tail -25",
    "ls -la /var/www/qrbanner/.next/server/pages/_error.js && echo ERROR_JS_OK",
    "pm2 restart qrbanner --update-env && sleep 4 && pm2 jlist 2>/dev/null | node -e \"let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{const a=JSON.parse(d);a.forEach(p=>console.log('PM2',p.name,'status=',p.pm2_env.status,'restarts=',p.pm2_env.restart_time))})\""
  ]
}
