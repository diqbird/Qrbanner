{
  "upload": [
    "lib/api-public-routes.ts",
    "lib/auth-options.ts"
  ],
  "yarn_install": false,
  "prisma_generate": false,
  "prisma_migrate": false,
  "build": false,
  "restart": false,
  "extra_commands": [
    "cd /var/www/qrbanner && rm -rf .next && yarn build 2>&1 | tail -10",
    "pm2 restart qrbanner --update-env && sleep 4",
    "cd /var/www/qrbanner && TEST_PASSWORD='112358Onrks..' node scripts/test-prod-login.mjs 2>&1 | tail -10"
  ]
}
