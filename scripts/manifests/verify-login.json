{
  "upload": ["scripts/test-prod-login.mjs"],
  "yarn_install": false,
  "prisma_generate": false,
  "prisma_migrate": false,
  "build": false,
  "restart": false,
  "extra_commands": [
    "curl -s 'https://qrbanner.com/api/auth/csrf' | head -c 120 ; echo",
    "curl -s -D - 'https://qrbanner.com/api/auth/csrf' -o /dev/null | grep -i 'HTTP/\\|set-cookie' | head -5",
    "cd /var/www/qrbanner && TEST_PASSWORD='112358Onrks..' node scripts/test-prod-login.mjs 2>&1 | tail -12"
  ]
}
