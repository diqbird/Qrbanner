/** Rolling window for “last 7 days” analytics stats. */
export const ANALYTICS_ROLLING_7_DAYS = 7;

/** Rolling window for “last 30 days” analytics stats. */
export const ANALYTICS_ROLLING_30_DAYS = 30;

/** Live scan feed polling interval shown in UI copy (seconds). */
export const LIVE_SCAN_REFRESH_SECONDS = 30;
