/** Minimum password length enforced at signup, reset and change. */
export const PASSWORD_MIN_LENGTH = 8;

/** Length at which the strength meter awards the second point. */
export const PASSWORD_STRONG_LENGTH = 12;
