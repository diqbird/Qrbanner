/** Email verification code validity (minutes). */
export const VERIFICATION_CODE_TTL_MINUTES = 30;

export const VERIFICATION_CODE_TTL_MS = VERIFICATION_CODE_TTL_MINUTES * 60 * 1000;

/** Password reset code validity (minutes). */
export const PASSWORD_RESET_CODE_TTL_MINUTES = 15;

export const PASSWORD_RESET_CODE_TTL_MS = PASSWORD_RESET_CODE_TTL_MINUTES * 60 * 1000;
