/** Target time to create first QR shown in onboarding copy (seconds). */
export const ONBOARDING_GOAL_SECONDS = 60;

/** Upper-bound ETA shown beside the onboarding flow (minutes). */
export const ONBOARDING_ETA_MAX_MINUTES = 1;
