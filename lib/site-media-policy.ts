/** Typical DNS propagation window cited in custom-domain setup copy. */
export const DNS_PROPAGATION_MAX_HOURS = 48;

/** Recommended Open Graph image dimensions (px). */
export const OG_IMAGE_RECOMMENDED_WIDTH = 1200;

export const OG_IMAGE_RECOMMENDED_HEIGHT = 630;
